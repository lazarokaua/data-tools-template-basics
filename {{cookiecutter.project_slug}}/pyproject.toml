[project]
name = "{{cookiecutter.project_name}}"
version = "0.1.0"
description = "Projeto de análise de dados"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "streamlit"
]

[tool.uv]
# uv usa isso pra gerenciar ambiente
